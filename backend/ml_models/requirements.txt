# Agro AI — Python dependencies for AI inference

# ─── Render / Production ──────────────────────────────────────────────────────
# onnxruntime-cpu = pure CPU-only build (no GPU code, no CUDA, no crashes on Render)
# DO NOT use plain 'onnxruntime' on Render — it loads CUDA drivers and crashes.
onnxruntime-cpu>=1.17.0
Pillow>=9.0.0
numpy>=1.24.0
